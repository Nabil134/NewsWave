import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF3289EF);
  static const Color primaryDark = Color(0xFF1E6FCB);
  static const Color primaryLight = Color(0xFF5AA3F5);

  // Background
  static const Color darkBackground = Color(0xFF1A1A2E);
  static const Color lightBackground = Color(0xFFF5F5F5);

  // Text
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Colors.white70;

  // Cards
  static const Color card = Colors.white;
  static const Color cardDark = Color(0xFF24243A);

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);

  // Other
  static const Color divider = Color(0xFFE5E7EB);
  static const Color transparent = Colors.transparent;
  static const Color progressBackground = Colors.white24;
}